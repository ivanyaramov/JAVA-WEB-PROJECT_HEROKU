spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/projectTEST?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&serverTimezone=UTC"
    username: root
    password: A123456789Z!
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: update
schedulers:
  cron: "0 0 1 * * ?"